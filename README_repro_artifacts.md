# EarSense Reproducibility Artifacts

This file is retained for compatibility with the original artifact staging
folder. The public-facing instructions are now in:

```text
README.md
```

Use `README.md` as the canonical guide for repository layout, recommended
commands, released model weights, demo data, and caveats about legacy scripts.
